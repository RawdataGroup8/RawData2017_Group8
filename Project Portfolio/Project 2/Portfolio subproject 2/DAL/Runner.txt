using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DAL
{
    class Runner
    {
        private static void Main(string[] args)
        {
            var con = new StackoverflowContext();
            var a = con.Comment.ToList().GetRange(0,50);
            foreach (var x in a)
            {
                Console.WriteLine(x.CommentText);
            }
            con.Comment.ToList();





        }

    }
}
